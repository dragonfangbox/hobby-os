#ifndef REGS_H
#define REGS_H

typedef struct {
	// this should contain all the registers needed to store 
	// and restore a process during an interrupt or something
	
} __attribute__((packed)) regs;

#endif
